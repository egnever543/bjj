UPDATE academia.presencas 
SET 
  status = 'rejeitado',
  aprovado_por = {{appsmith.store.usuarioLogado.id}},
  aprovado_em = NOW()
WHERE id = {{List_pendentes.triggeredItem.presenca_id}}
RETURNING *;